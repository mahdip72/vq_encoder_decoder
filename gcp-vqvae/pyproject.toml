[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gcp-vqvae"
version = "0.2.2"
description = "GCP-VQVAE inference wrapper (encode/embed/decode)"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}

dependencies = [
  "torch",
  "numpy",
  "pyyaml",
  "python-box",
  "tqdm",
  "huggingface-hub",
  "h5py",
  "transformers",
  "x-transformers==2.8.0",
  "vector-quantize-pytorch==1.25.2",
  "ndlinear",
  "graphein",
  "biopython",
  "torch-geometric",
  "torch-cluster",
  "torch-scatter",
]


[tool.setuptools.packages.find]
where = ["."]
include = ["gcp_vqvae", "gcp_vqvae.*"]
